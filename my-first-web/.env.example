# Sao chép: cp .env.example .env  rồi điền giá trị thật

# ── Server ──
PORT=3000
NODE_ENV=production
BRAIN_DB_PATH=./my-brain/brain.db

# ── Supabase (anon key — bật RLS chặt trên dashboard) ──
SUPABASE_URL=https://YOUR_PROJECT.supabase.co
SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_LEADS_TABLE=leads

# ── Thanh toán VietQR (hiển thị trên web) ──
PAYMENT_BANK_ID=BIDV
PAYMENT_ACCOUNT_NO=
PAYMENT_ACCOUNT_NAME=

# ── Admin panel (client + header X-Admin-Password cho API brain) ──
ADMIN_PASSWORD=doi-mat-khau-manh

# ── Liên kết công khai (tùy chọn) ──
ZALO_LINK=https://zalo.me/moawmoaws
WAITLIST_FORM_URL=https://docs.google.com/forms/d/e/...

# ── Chỉ Supabase Edge Function Secrets (không đưa vào js/config.js) ──
RESEND_API_KEY=re_...

# --- OpenAI (Cho Gen Content / Creative) ---
OPENAI_API_KEY=sk-proj-...
